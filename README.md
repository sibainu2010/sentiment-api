# Sentiment API

FastAPI-сервис для анализа тональности русскоязычных отзывов.

## Запуск локально

```bash
pip install -r requirements.txt
uvicorn app.main:app --reload
```

## Деплой

Проект настроен для деплоя на [Render](https://render.com) через `render.yaml`.
